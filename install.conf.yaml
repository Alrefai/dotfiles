- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.gitconfig:
    ~/.zshrc:
    ~/.tvnamer.json:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
